name: 01 - Terraform Plan

# متى يشتغل: عند عمل Pull Request على main
on:
  pull_request:
    branches:
      - main
    paths:
      - 'terraform-k8s-infrastructure/**'

# المتغيرات العامة
env:
  TF_VERSION: '1.6.0'
  AWS_REGION: 'us-east-1'

jobs:
  terraform-plan:
    name: Review Infrastructure Changes
    runs-on: ubuntu-latest
    
    # الصلاحيات المطلوبة
    permissions:
      contents: read          # قراءة الكود
      pull-requests: write    # كتابة تعليق على PR
    
    steps:
      # ═══════════════════════════════════════
      # الخطوة 1: تحميل الكود
      # ═══════════════════════════════════════
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
      
      # ═══════════════════════════════════════
      # الخطوة 2: الاتصال بـ AWS
      # ═══════════════════════════════════════
      - name: 🔐 Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      # ═══════════════════════════════════════
      # الخطوة 3: تنصيب Terraform
      # ═══════════════════════════════════════
      - name: 🔧 Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}
      
      # ═══════════════════════════════════════
      # الخطوة 4: Terraform Format Check
      # ═══════════════════════════════════════
      - name: 🎨 Terraform Format Check
        id: fmt
        working-directory: terraform-k8s-infrastructure
        run: terraform fmt -check -recursive
        continue-on-error: true
      
      # ═══════════════════════════════════════
      # الخطوة 5: Terraform Init
      # ═══════════════════════════════════════
      - name: ⚙️ Terraform Init
        id: init
        working-directory: terraform-k8s-infrastructure
        run: terraform init
      
      # ═══════════════════════════════════════
      # الخطوة 6: Terraform Validate
      # ═══════════════════════════════════════
      - name: ✅ Terraform Validate
        id: validate
        working-directory: terraform-k8s-infrastructure
        run: terraform validate -no-color
      
      # ═══════════════════════════════════════
      # الخطوة 7: Terraform Plan
      # ═══════════════════════════════════════
      - name: 📋 Terraform Plan
        id: plan
        working-directory: terraform-k8s-infrastructure
        run: terraform plan -no-color -input=false
        continue-on-error: true
      
      # ═══════════════════════════════════════
      # الخطوة 8: عرض النتائج على PR
      # ═══════════════════════════════════════
      - name: 💬 Comment on Pull Request
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request'
        env:
          PLAN: "${{ steps.plan.outputs.stdout }}"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `
            ## 🏗️ Terraform Plan Results
            
            #### 🎨 Format Check: \`${{ steps.fmt.outcome }}\`
            #### ⚙️ Initialization: \`${{ steps.init.outcome }}\`
            #### ✅ Validation: \`${{ steps.validate.outcome }}\`
            #### 📋 Plan: \`${{ steps.plan.outcome }}\`

            <details><summary>📖 Show Plan Details</summary>

            \`\`\`terraform
            ${process.env.PLAN}
            \`\`\`

            </details>

            **👤 Triggered by:** @${{ github.actor }}
            **📅 Date:** ${new Date().toLocaleString('en-US', { timeZone: 'Africa/Cairo' })}
            `;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })
      
      # ═══════════════════════════════════════
      # الخطوة 9: فشل إذا كان Plan فشل
      # ═══════════════════════════════════════
      - name: ❌ Fail if Plan Failed
        if: steps.plan.outcome == 'failure'
        run: exit 1